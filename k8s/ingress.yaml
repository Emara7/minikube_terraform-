apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-native-demo
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: cloud-native-demo.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cloud-native-demo
                port:
                  number: 80
